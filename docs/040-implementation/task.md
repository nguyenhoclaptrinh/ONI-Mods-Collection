# Danh sách tác vụ tối ưu Reflector

- [x] Tạo file cấu hình `tools/Reflector/config.json` chứa đường dẫn game.
- [x] Viết lại `tools/Reflector/Program.cs` để đọc `config.json` và nhận tên Class qua `args`.
- [x] Triển khai hàm phản chiếu đầy đủ và xuất định dạng markdown chuẩn YAML Frontmatter.
- [x] Chạy kiểm thử phản chiếu `KCrashReporter` và `SaveLoader`.
- [x] Xác minh các file markdown được sinh ra trong `docs/050-Research/outputs/` và chạy ổn định.
